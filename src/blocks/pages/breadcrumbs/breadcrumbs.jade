//- include ../blocks/pages/breadcrumbs/breadcrumbs.jade
mixin bread(options)
	ul.breadcrumbs
		each item in options
			if item.current
				li.breadcrumbs__item.breadcrumbs__item--current: a.breadcrumbs__link(href="#")= item.name
			else
				li.breadcrumbs__item: a.breadcrumbs__link(href=item.href)= item.name
